{
  "name": "backend-express-template-simple",
  "version": "1.0.0",
  "description": "Simple backend template for Express deliverables",
  "main": "app.js",
  "scripts": {
    "test": "jest --verbose --runInBand --testLocationInResults --setupFiles dotenv/config",
    "test:watch": "npm run test -- --watch",
    "start": "node -r dotenv/config server.js",
    "start:watch": "nodemon -r dotenv/config server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "dotenv": "^16.0.3",
    "nodemon": "^2.0.20",
    "eslint": "^8.26.0",
    "jest": "^29.2.2",
    "prettier": "^2.7.1",
    "supertest": "^6.3.1"
  }
}
